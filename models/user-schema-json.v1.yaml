title: user-schema-json
type: object
properties:
  firstName:
    type: string
  lastName:
    type: string
  email:
    type: string
    description: Email is required and used for login
  password:
    type: string
    description: 'Password is used, saved as hashed at registration and used for login'
  phoneNumber:
    type: string
  createdAt:
    type: string
    description: Date type
required:
  - email
  - password
